{
  "name": "edutalk-webapp",
  "version": "1.0.0",
  "description": "Simple Node + static frontend for Azure App Service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests\" && exit 0"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
